# AWS-Cloud-Dashboard

# Create React app
npx create-react-app cloud-infrastructure-monitor
cd cloud-infrastructure-monitor

# Install dependencies
npm install lucide-react

# Replace src/App.js with CloudMonitorDashboard.js content

# Add to src/index.css (Tailwind CDN)
# Already included in the component

# Test locally
npm start

# Initialize git and push to GitHub
git init
git add .
git commit -m "Initial commit: Cloud Infrastructure Monitor Dashboard"
git branch -M main
git remote add origin https://github.com/sravyasambaturu/cloud-infrastructure-monitor.git
git push -u origin main
